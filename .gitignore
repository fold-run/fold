/fold
/dist/

# Local compose stack: the operator's own config, the generated SHIM_TOKEN,
# and whatever the filesystem server is pointed at.
/fold.config.json
/.env
/data/
*.log
*.test
coverage.out

# Claude Code personal overrides
.claude/settings.local.json

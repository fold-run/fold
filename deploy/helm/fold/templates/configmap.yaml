{{- if .Values.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fold.configMapName" . }}
  labels:
    {{- include "fold.labels" . | nindent 4 }}
data:
  fold.config.json: {{ .Values.config | toPrettyJson | quote }}
{{- end }}

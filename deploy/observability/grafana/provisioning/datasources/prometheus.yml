# Provisioned so the stack comes up ready: no click-through, and the dashboard
# resolves its ${datasource} variable to this on first load.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
